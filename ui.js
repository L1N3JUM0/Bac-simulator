/* ============================================================================
   SimuBac — ui.js
   ----------------------------------------------------------------------------
   COUCHE DE RENDU : construit le DOM dynamique (listes de spécialités,
   d'options, lignes de notes adaptées au parcours), affiche les résultats,
   gère les interactions de formulaire et les graphiques Chart.js.

   ÉTAPE 3 — rendu dynamique et liaison état ↔ interface.
   ÉTAPE 4 — graphiques (Chart.js embarqué dans assets/libs/).

   Règle d'or : ui.js LIT les résultats produits par calculator.js,
   il ne calcule jamais lui-même.
   ============================================================================ */

// Étape 3 : implémentation du rendu.
export {};
