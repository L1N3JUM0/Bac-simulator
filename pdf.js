/* ============================================================================
   SimuBac — pdf.js
   ----------------------------------------------------------------------------
   EXPORT PDF : génère le bilan complet (identité, date, notes, coefficients,
   calculs, scénarios) et y insère les graphiques capturés depuis les canvas
   Chart.js via canvas.toDataURL().

   Bibliothèque : jsPDF, embarquée localement dans assets/libs/ (étape 5)
   pour garantir le fonctionnement hors ligne.

   ÉTAPE 5 — fonction prévue :
     genererPDF(state, resultats) → télécharge "simubac-bilan.pdf"
   ============================================================================ */

// Étape 5 : implémentation de l'export.
export {};
